import React from "react";

describe("ForcastSummary", () => {
  const validProps = {
    date: 111111,
    description: "Stub Description",
    icon: "stubIcon",
    temperature: {
      min: 12,
      max: 22,
    },
  };

  it("Renders ok", () => {});
});
